# INSTALL

```
docker run -it --name picoFollower -p 3001:3000 node:latest /bin/bash

apt-get update -y
apt-get install -y vim gzip git-core curl python libssl-dev pkg-config build-essential supervisor
apt-get install -y node-express-generator mongodb libcairo2-dev  libpango1.0-dev libgif-dev g++
apt-get install -y cron

npm install express

express eatinggoodtogether
cd eatinggoodtogether
npm install
npm install -d
npm install mongodb --save
npm install mongo-sanitize mongoose --save
npm install bluebird nodemailer --save
npm install xss-filters --save
npm install --save formidable
npm install --save express-session




```